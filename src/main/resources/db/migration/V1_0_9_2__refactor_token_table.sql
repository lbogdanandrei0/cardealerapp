drop table token;
drop table token_test;

create table token(
    id int primary key generated by default as identity,
    token varchar(70) not null,
    email varchar(100),
    expiry_date timestamp default current_timestamp + (10 || 'minutes')::interval
);